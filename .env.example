NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Base URL pubblica (invite email redirect, es. https://hub.reparto.it — aggiungi anche tra Redirect URLs nel pannello Supabase Auth).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Solo debug: se 1, dopo invite se admin_apply_profile_update fallisce NON viene chiamato deleteUser (rimane Auth + link invito). Toglierlo subito dopo.
# DEBUG_SKIP_INVITE_ROLLBACK_ON_RPC_FAIL=1
